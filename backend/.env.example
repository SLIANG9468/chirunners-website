# Copy this file to `.env` and fill in your real values.
# Never commit `.env` to git.

MAPBOX_ACCESS_TOKEN=pk.your_mapbox_token_here

# Matches frontend API URLs (localhost:5001). Override if needed.
FLASK_PORT=5001
