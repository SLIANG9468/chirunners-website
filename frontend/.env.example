# Vite: only variables prefixed with VITE_ are exposed to the client.
# Local dev: default in code is http://localhost:5001 if unset.
# Production (Render static site): set to your API URL, no trailing slash.
# VITE_API_BASE_URL=https://chirunners-api.onrender.com
